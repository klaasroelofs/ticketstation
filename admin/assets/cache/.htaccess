# Deny direct web access to the QR-code generation cache. Files here are transient and
# only meant to be read server-side while a PDF is being assembled.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
